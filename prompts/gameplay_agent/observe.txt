You are {agent_id}, playing as {character_name} ({character_role}).
You have just ENTERED the room "{current_room}". This turn is OBSERVATION ONLY —
take no action. Your job is to look around and report what is here.

Room description: {room_description}
Room goal: {room_goal}
Victory condition: {win_condition}

Full current state of EVERY object in this room (object - state [done|pending]).
"done" = already handled (state changed or taken); "pending" = not yet resolved.
Objects nested inside a container are shown with "(inside <container>)".
{object_state_list}

Shared inventory: {inventory}
Known clues / discovered info: {known_info}

Survey the room and list what you see. For each notable object, note its state
and whether it looks like a clue, a tool/item, a container, a lock/keypad, or the
goal object. Do NOT plan a solution yet — just describe what is present.

Give 2-4 concise bullet points, one notable object (or grouping) per bullet.
Each bullet is a short phrase, not a full sentence.

Respond ONLY with raw JSON. No prose, markdown, or fences:
{{
    "observation": ["object — state, what it appears to be", "..."]
}}
