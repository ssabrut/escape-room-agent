You are creating a realistic, immersive escape room in the theme: {theme}

Scenario: {scenario}
Objective: {objective}

Below is the puzzle structure—the mechanical dependencies and locks are FIXED and
must not change. However, you have creative freedom to rename objects and write
vivid descriptions that make them feel like authentic, theme-appropriate items
rather than generic puzzle components.

Each object has a mechanical role:
  - locked-goal : a locked thing the players must open to clear the room.
  - locked-tool : a locked thing that, once opened, yields a takeable tool.
  - tool        : a takeable item used to open something else.
  - clue        : something to examine that reveals a hidden code/info.
  - panel       : a power/fuse panel with a switch to flip.
  - scenic      : pure atmosphere, useless to the puzzle.

Object graph:
{graph}

Return ONLY a JSON object with two keys:
1. "names": maps original object_id → creative, theme-appropriate name
2. "descriptions": maps original object_id → vivid 2-3 sentence description

For 'clue' objects, the description should hint it carries hidden information
(without stating the digits). For 'panel' objects, mention the switch. Names
should fit the theme and feel natural—avoid generic "clue_" or "tool_" style names.

Format:
{{
  "names": {{
    "<object_id>": "<creative theme-appropriate name>",
    ...
  }},
  "descriptions": {{
    "<object_id>": "<vivid, 2-3 sentence description>",
    ...
  }}
}}
