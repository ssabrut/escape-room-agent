Generate the setting for a HARD 2D text-based conversational escape room game. This is for an automated difficulty benchmark — prioritize a deeply atmospheric, coherent world that a complex puzzle chain can be built upon.

Theme: {theme}
Number of rooms: {num_rooms} (a linear chain: room 1 is the start, room {num_rooms} is the final room)

NARRATIVE QUALITY:
* Each room description must paint the space with at least two vivid sensory details.
* Rooms must feel like they belong to the same story world: consistent tone, era, and stakes.
* The objective must create genuine tension — something is at stake beyond "escape".

Include:
* scenario (one short paragraph)
* objective (one sentence)
* Exactly {num_rooms} rooms in a linear chain.

Each room must have:
- id (unique snake_case)
- description (vivid, at least two sensory details)
- adjacency: connect room i to room i+1 with a mirrored direction pair (north<->south or east<->west). Each room references only its chain neighbors.
- goal: one sentence describing what the party must accomplish here. Each non-final room's goal gates passage to the next room. The final room's goal ends the game.
- goal_completion: a structured condition that is TRUE exactly when the room's goal is done. Must require non-trivial effort.
  Shapes:
    - {{"type": "object_state", "object_id": "<id>", "state": "unlocked"}}  ← use literal "unlocked" for any open/unlock goal
    - {{"type": "known_info", "info": "<token>"}}
    - {{"type": "has_item", "object_id": "<id>"}}
    - {{"type": "power_active", "id": "<token>"}}
- key_objects: list of 2–4 descriptive placeholder ids for the most important puzzle objects expected in this room. These will be placed during puzzle generation.

The FINAL room's goal_completion will become the win condition.

Difficulty target: the puzzle chain built on top of this setting must support at least {chain_depth} dependent sequential steps. Design room goals and goal_completion conditions that naturally demand a deep chain (e.g. a goal_completion that requires a code, which requires a tool, which requires another clue...).

Respond ONLY with raw JSON — no prose, no markdown fences.

{{
    "scenario": "one paragraph",
    "objective": "one sentence",
    "rooms": [
        {{"id": "room_1", "description": "...", "adjacency": {{"east": "room_2"}}, "goal": "...", "goal_completion": {{"type": "object_state", "object_id": "door_1", "state": "unlocked"}}, "key_objects": ["room1_gate", "room1_clue"]}}
    ]
}}