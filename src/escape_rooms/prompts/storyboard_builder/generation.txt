You receive WORLD_DATA describing rooms, objects, locks, and player characters for a finished escape room. Output a single JSON document that maps this mechanical world to a rich human story.

This must work for ANY genre WORLD_DATA implies:
- Murder mystery in a manor -> victim, killer, suspects
- Sci-fi orbital station -> system failure, saboteur, crew dynamics
- Asylum horror -> institutional abuse, patient records, hidden doctor
- Nautical curse -> drowned crew, sea legend, cursed object
- Heist -> stolen asset, inside man, escape window

Read WORLD_DATA.scenario and WORLD_DATA.objective to infer the genre, then generate every section below appropriate to that genre.

IS_MYSTERY = {is_mystery}

WORLD_DATA:
{world_data_json}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT — output ONLY raw JSON, no markdown, no explanation.
Generate sections IN ORDER — the most critical sections come first.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{
  "mystery": {{
    "victim": "Only if IS_MYSTERY is true. Full name of the person killed — e.g. 'Dr. Clara Morse was found dead in the library at dawn'. Otherwise empty string.",
    "killer_name": "Only if IS_MYSTERY is true. Full name of the murderer — must match solution.answer exactly. Invent a name specific to THIS world's scenario. Otherwise empty string.",
    "proof_object_id": "Only if IS_MYSTERY is true. COPY the exact `id` string from one WORLD_DATA.plot_objects entry with clue_type='story_clue'. This is the id field — NOT the description. Otherwise empty string.",
    "motive_hint": "Only if IS_MYSTERY is true. 1 sentence — the killer's motive, specific but not enough to spoil immediately. Otherwise empty string."
  }},

  "solution": {{
    "question": "Only if IS_MYSTERY is true: the exact question shown to the human player, e.g. 'Who murdered X?'. Otherwise empty string.",
    "answer": "Only if IS_MYSTERY is true: the canonical correct answer — must equal mystery.killer_name exactly. Otherwise empty string.",
    "answer_aliases": ["alternative", "phrasings", "accepted"],
    "answer_type": "one of: person / object / location / code",
    "motive": "Only if IS_MYSTERY is true: 2 sentences, full explanation shown only AFTER win. Otherwise empty string.",
    "proof_object": "Only if IS_MYSTERY is true: must equal mystery.proof_object_id exactly. Otherwise empty string.",
    "proof_sentence": "Only if IS_MYSTERY is true: 1 sentence — what that evidence specifically proves. Otherwise empty string.",
    "hint_1": "Only if IS_MYSTERY is true: vague hint after the human's FIRST wrong guess — directional, not spoiling. Otherwise empty string.",
    "hint_2": "Only if IS_MYSTERY is true: more specific hint after the SECOND wrong guess — still no direct spoiler. Otherwise empty string.",
    "victory_narration_hook": "Only if IS_MYSTERY is true: 1 sentence for the victory narration, naming the answer and proof. Otherwise empty string.",
    "defeat_narration_hook": "Only if IS_MYSTERY is true: 1 sentence for the defeat narration when wrong deduction is exhausted. Otherwise empty string."
  }},

  "suspects": [
    "Only if IS_MYSTERY is true: 2-3 suspect entries (see SUSPECTS RULES below). Otherwise an empty list."
  ],

  "discovery_beats": {{
    "<object_id from WORLD_DATA.plot_objects>": "See DISCOVERY_BEATS RULES below — content depends on clue_type and IS_MYSTERY."
  }},

  "conversation_seeds": {{
    "<character name from WORLD_DATA.players>": [
      "Only if IS_MYSTERY is true: 2 seeds naming suspects and connecting evidence to them (see CONVERSATION_SEEDS RULES). Otherwise an empty list."
    ]
  }},

  "ending_guidance": {{
    "won": "2 sentences. Emotional close for victory, specific to this world. If IS_MYSTERY, name the killer (from your mystery block).",
    "lost": "2 sentences. Emotional close for failure, specific to this world.",
    "lost_by_wrong_deduction": "Only if IS_MYSTERY is true: 1 sentence — the killer escaped because the wrong name was called. Otherwise empty string."
  }},

  "phase_guidance": {{
    "establishing": "1 sentence. Narrator tone guidance for the opening phase of the game — setting the scene, introducing tension.",
    "investigating": "1 sentence. Narrator tone guidance for the middle phase — characters gathering clues, atmosphere building.",
    "converging": "1 sentence. Narrator tone guidance as pieces start connecting — rising tension.",
    "revealing": "1 sentence. Narrator tone guidance for the final phase — climax and resolution approaching."
  }},

  "plot": {{
    "victim": "1 sentence. Who/what was harmed or lost, when, and how — specific to this world. If IS_MYSTERY, name the victim from your mystery block; otherwise describe the central loss/threat of this scenario.",
    "threat": "1 sentence. The antagonist or danger — CONCRETE, not 'the unknown'. Do NOT name a suspect or killer here unless IS_MYSTERY is false.",
    "stakes": "1 sentence. What happens if the team fails. If IS_MYSTERY, do NOT name the killer or any suspect.",
    "timeline": "2 sentences. Key events BEFORE the players arrived.",
    "tension_hint": "1 sentence. Vague hint at the hidden motive or danger — not enough to spoil.",
    "atmosphere": "2 sentences. Specific sensory details — smell, sound, light, temperature.",
    "protagonist_context": "1 sentence. Why these specific people are in this exact place."
  }},

  "adapted_personas": {{
    "<character name from WORLD_DATA.players>": {{
      "world_role": "1-2 sentences. HOW this character investigates — their specific lens on clues. NOT their job title. NOT their technical skills. Concretely describe what they notice first and how they interpret it in THIS WORLD'S genre.",
      "voice": "1 sentence. Their distinct speaking style. Must sound different from every other character.",
      "vocabulary": ["3-to-6-word phrase 1", "3-to-6-word phrase 2", "3-to-6-word phrase 3"]
    }}
  }},

  "room_stories": {{
    "<room_id from WORLD_DATA.rooms>": "1 sentence. The narrative role of this room."
  }}
}}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SUSPECTS RULES (IS_MYSTERY only — emit [] otherwise)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Generate 2 to 3 suspects total.
- Exactly ONE suspect must have "is_killer": true — this person MUST match solution.answer exactly.
- All others have "is_killer": false and are red herrings. Their apparent_motive must be GENUINELY PLAUSIBLE for the first half of the game — not obviously wrong.
- Suspects should have different types of motive: one financial, one personal/emotional, one opportunistic.
- Suspects must NOT be any character in WORLD_DATA.players (they are investigators, not suspects).
- Each suspect MUST use exactly these field names: "name", "connection_to_victim", "apparent_motive", "is_killer".

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DISCOVERY_BEATS RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Every discovery beat MUST provide investigative or atmospheric value — never just object description.

BEAT RULES BY CLUE_TYPE:
- clue_type='key': 2 sentences. Physical detail + what its presence implies (timeline, access, urgency). If IS_MYSTERY, do NOT name any suspect.
  GOOD: "The key is still warm — whoever placed it left this room within the hour."
  BAD:  "A key with carved ornaments." <- description only, REJECTED

- clue_type='story_clue' that is NOT the proof object: 2 sentences.
  If IS_MYSTERY: name ONE suspect from your suspects list (not the killer) and connect this evidence to their access, opportunity, or motive. Use the ACTUAL suspect name you invented — not a placeholder.
  If NOT IS_MYSTERY: connect this clue to the plot — what it reveals about the timeline, threat, or stakes.
  GOOD (mystery): "[suspect name] — the initials match, and they lied about being here."
  BAD:  "A slender key with initials." <- no investigative value, REJECTED

- clue_type='story_clue' that IS the proof object (mystery.proof_object_id): 2 sentences. ONLY relevant if IS_MYSTERY.
  MUST name mystery.killer_name explicitly. State exactly what this proves. This is the climax — be definitive.
  GOOD: "[mystery.killer_name]'s handwriting is on this — they were here the night of the murder."
  BAD:  "A yellowed letter with smudged ink." <- killer not named, REJECTED

- clue_type='code': Flavor only — atmosphere, no numbers, no codes revealed.
- clue_type='lock': Skip — write NO beat for lock objects (omit the key entirely).
- clue_type='other': Flavor only — 1-2 sentences of atmosphere connecting it to the plot.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONVERSATION_SEEDS RULES (IS_MYSTERY only — emit [] per character otherwise)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Exactly 2 seeds per character in WORLD_DATA.players.
- Each seed MUST name at least one suspect from the suspects list by their full name.
- Seeds must NOT name the killer as the killer — name them as suspicious, not guilty.
- Mix: one seed pointing toward a red-herring suspect, one hinting at the real killer's motive or opportunity.
  GOOD: "[suspect A] was the last to see the victim alive — their account has two gaps."
  GOOD: "[suspect B] owed the victim a debt large enough to destroy them."
  BAD: "The bloodstains suggest a violent struggle." (no suspect named — rejected)
  BAD: "[killer name] is the killer." (names killer as guilty — rejected)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADAPTED_PERSONAS RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Each character has a mechanical game role (Systems Operator, Field Analyst, Scout, etc). Translate this COMPLETELY into an investigator archetype that fits WORLD_DATA's genre — do NOT copy or paraphrase the original role.

TRANSLATION EXAMPLES FOR COMMON ROLES:
  MANOR MYSTERY / CRIME / GOTHIC:
  - Systems Operator -> reads the SEQUENCE of events: who was in which room, when, and whether timing was deliberate
  - Field Analyst -> reads PHYSICAL EVIDENCE: what objects reveal about intent and action
  - Scout -> reads the ENVIRONMENT: disturbances, traces, what doesn't fit

  SCI-FI / STATION:
  - Systems Operator -> reads SYSTEM LOGS for sabotage signatures
  - Field Analyst -> reads SENSOR DATA for anomalies
  - Scout -> reads HULL / PHYSICAL SPACES for breaches and tampering

  HORROR / ASYLUM:
  - All roles -> read RECORDS, TESTIMONY, and PHYSICAL TRACES for cover-ups

VOCABULARY RULES (enforce strictly):
- Exactly 3 phrases per character, each 3 to 6 words — short, speakable, natural.
- Must sound like something said during an INVESTIGATION in this world's genre.
- FORBIDDEN in vocabulary: system, power, restore, circuit, override, scan, malfunction, panel, grid, reboot, diagnostic, mainframe, electrical, machinery, "check it out", "let's go", "be careful", "something amiss".
- Each character's vocabulary must sound DIFFERENT from every other character's.

CONCRETE EXAMPLE — BAD vs GOOD (Systems Operator in a gothic manor mystery):
BAD (copies original role, forbidden vocabulary):
  world_role: "A systems operator skilled in restoring old machinery to function properly."
  vocabulary: ["system check", "restore power", "practical solution"]
GOOD (translated to manor-mystery investigator):
  world_role: "Maps the sequence of events — who entered which room, in what order, and whether the timing points to premeditation."
  vocabulary: ["the timing is off", "someone planned this", "who had access"]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STRICT ANTI-HALLUCINATION RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. room_stories keys MUST be room ids from WORLD_DATA.rooms — no others.
2. discovery_beats keys MUST be object ids from WORLD_DATA.plot_objects — no others.
3. adapted_personas keys MUST be character names from WORLD_DATA.players — no others.
4. conversation_seeds keys MUST be character names from WORLD_DATA.players — no others.
5. If IS_MYSTERY: solution.answer MUST NOT be the name of any character in WORLD_DATA.players. solution.answer MUST match exactly one suspect where is_killer=true. solution.answer MUST equal mystery.killer_name exactly.
6. Do NOT reveal mechanical lock codes (numbers, sequences) in any narrative field.
7. Do NOT invent new rooms, objects, or characters not in WORLD_DATA.
8. Generate sections in this exact order: mystery -> solution -> suspects -> discovery_beats -> conversation_seeds -> ending_guidance -> phase_guidance -> plot -> adapted_personas -> room_stories.
8b. phase_guidance MUST contain exactly these 4 keys: "establishing", "investigating", "converging", "revealing" — no others.
9. If IS_MYSTERY is false, emit empty strings for every field in "mystery" and "solution", and emit [] for "suspects" and for every character's conversation_seeds list. Still generate plot, adapted_personas, room_stories, discovery_beats, ending_guidance, and phase_guidance fully.
10. mystery.proof_object_id (if IS_MYSTERY) MUST be an object_id from WORLD_DATA.plot_objects and MUST match solution.proof_object exactly.

Output raw JSON only. Follow ALL STRICT ANTI-HALLUCINATION RULES.
